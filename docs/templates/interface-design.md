# 接口 / 协议 / 设备设计: [FEATURE_NAME]

> **Author: HUMAN 定义 / CLAUDE 可补充示例**
> Claude 实现时严格遵循，不自行发挥接口风格。

## 设计类型

<!-- 勾选本需求属于哪类，删掉不相关的小节 -->
- [ ] 新设备驱动
- [ ] 新协议编译类型
- [ ] 调度/后端逻辑
- [ ] 通信桥接口

## 设备驱动设计（如适用）

| 项 | 说明 |
|----|------|
| 设备类型 | |
| 通信协议 | OPC-UA / Modbus / RS485 / 串口 / ... |
| 动作（actions） | |
| 状态（states） | |
| transport 抽象 | <!-- 必须可注入 fake，供 hermetic 测试 --> |

## 协议编译设计（如适用）

- 输入 YAML schema:
- 输出（编译目标）:
- **往返不变量**: <!-- 例：decompile(compile(x)) == x，供 Hypothesis --> 

## 调度/后端设计（如适用）

- 资源模型:
- 不变量: <!-- 例：任一时刻同一资源不被两个任务占用 -->
- 时钟注入点: <!-- 超时/重试逻辑如何注入可控时钟 -->

## 通信桥接口（如适用）

- 消息格式:
- 端点:

## 测试策略

- fake/mock 点:
- 需要 Hypothesis 覆盖的性质:
