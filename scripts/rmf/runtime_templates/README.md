# RMF Runtime Templates

This directory stores versioned runtime templates used by Phase5 bundle generation.

Current strategy:

- Canonical source: `Uni-Lab-OS/scripts/rmf/runtime_templates/`
- `build_runtime_bundle.py` only reads templates from this directory.

`/.rmf_run_logs/` is runtime output only and should not be used as template source.

