diff --git a/rmf_robot_sim_common/include/rmf_robot_sim_common/readonly_common.hpp b/rmf_robot_sim_common/include/rmf_robot_sim_common/readonly_common.hpp
index 3621daf..5137f06 100644
--- a/rmf_robot_sim_common/include/rmf_robot_sim_common/readonly_common.hpp
+++ b/rmf_robot_sim_common/include/rmf_robot_sim_common/readonly_common.hpp
@@ -122,7 +122,7 @@ void ReadonlyCommon::read_sdf(SdfPtrT& sdf)
   RCLCPP_INFO(logger(), "Setting level name to: %s", _level_name.c_str());
 
   if (sdf->HasElement("graph_index"))
-    _nav_graph_index = sdf->template Get<std::size_t>("graph_index");
+    _nav_graph_index = sdf->template Get<uint64_t>("graph_index");
   RCLCPP_INFO(logger(), "Setting nav graph index: %d", _nav_graph_index);
 
   if (sdf->HasElement("spawn_waypoint"))
@@ -130,7 +130,7 @@ void ReadonlyCommon::read_sdf(SdfPtrT& sdf)
   RCLCPP_INFO(logger(), "Setting start wp name: %s", _start_wp_name.c_str());
 
   if (sdf->HasElement("look_ahead"))
-    _lookahead = sdf->template Get<std::size_t>("look_ahead");
+    _lookahead = sdf->template Get<uint64_t>("look_ahead");
   _lookahead = _lookahead < 1 ? 1 : _lookahead;
   _next_wp.resize(_lookahead);
   _path.resize(_lookahead);
diff --git a/rmf_robot_sim_gz_plugins/CMakeLists.txt b/rmf_robot_sim_gz_plugins/CMakeLists.txt
index 352806f..495cfd2 100644
--- a/rmf_robot_sim_gz_plugins/CMakeLists.txt
+++ b/rmf_robot_sim_gz_plugins/CMakeLists.txt
@@ -152,34 +152,35 @@ target_include_directories(slotcar
 )
 
 ###############################
-# LightTuning
+# LightTuning (disabled on macOS: Qt MOC/GuiSystem link errors, not needed
+# for headless/demo launches)
 ###############################
 
-QT5_ADD_RESOURCES(resources_RCC src/LightTuning/LightTuning.qrc)
-
-add_library(LightTuning SHARED ${headers_MOC}
-  src/LightTuning/LightTuning.cpp
-  ${resources_RCC}
-)
-
-ament_target_dependencies(LightTuning
-  ignition-gui${IGN_GUI_VER}
-  ignition-gazebo${IGN_GAZEBO_VER}
-  ignition-msgs${IGN_MSGS_VER}
-  ignition-transport${IGN_TRANSPORT_VER}
-  ignition-rendering${IGN_RENDERING_VER}
-  Qt5Core
-  Qt5Qml
-  Qt5Quick
-  rclcpp
-)
-
-target_include_directories(LightTuning
-  PUBLIC
-  ${Qt5Core_INCLUDE_DIRS}
-  ${Qt5Qml_INCLUDE_DIRS}
-  ${Qt5Quick_INCLUDE_DIRS}
-)
+# QT5_ADD_RESOURCES(resources_RCC src/LightTuning/LightTuning.qrc)
+#
+# add_library(LightTuning SHARED ${headers_MOC}
+#   src/LightTuning/LightTuning.cpp
+#   ${resources_RCC}
+# )
+#
+# ament_target_dependencies(LightTuning
+#   ignition-gui${IGN_GUI_VER}
+#   ignition-gazebo${IGN_GAZEBO_VER}
+#   ignition-msgs${IGN_MSGS_VER}
+#   ignition-transport${IGN_TRANSPORT_VER}
+#   ignition-rendering${IGN_RENDERING_VER}
+#   Qt5Core
+#   Qt5Qml
+#   Qt5Quick
+#   rclcpp
+# )
+#
+# target_include_directories(LightTuning
+#   PUBLIC
+#   ${Qt5Core_INCLUDE_DIRS}
+#   ${Qt5Qml_INCLUDE_DIRS}
+#   ${Qt5Quick_INCLUDE_DIRS}
+# )
 
 ###############################
 # Install Targets
@@ -191,7 +192,6 @@ install(
     teleport_ingestor
     readonly
     slotcar
-    LightTuning
   LIBRARY DESTINATION lib/${PROJECT_NAME}
   ARCHIVE DESTINATION lib/${PROJECT_NAME}
 )
